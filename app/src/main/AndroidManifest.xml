<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.loan.loaneazy">

  
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.hardwre.location.gps" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="false"
        android:smallScreens="true" />

    <application
        android:name=".Apps"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_loan_round"
        android:label="@string/app_name"
        android:largeHeap="true"
    
      
        <activity
            android:name=".activity.profile.FeedbackActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DeerPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />
      
        </receiver>
        -->
      
        <activity
            android:name=".activity.certificate.SingleContactActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DeerPay.NoActionBar" />
        <activity
            android:name=".activity.profile.AboutUsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DeerPay.NoActionBar" />
    

        <receiver
            android:name=".sdk.EpochDataSyncReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.yinda.datasyc.app_init.DATASYNC_BROADCAST_ACTION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>